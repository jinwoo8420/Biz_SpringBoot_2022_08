# DBMS setup
## Data Source 설정
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=!Korea8080
spring.datasource.url=jdbc:mysql://localhost:3306/bootDB

# create : table create after Deletion
# create-drop : table create AND Project Stop Deletion
# none
# update : Alter table NOT
# validate : Error occurred after inspection
spring.jpa.hibernate.ddl-auto=update

spring.thymeleaf.prefix=classpath:html/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.template-resolver-order=0
spring.thymeleaf.cache=false

# css, js, image등을 공개 할 디렉토리 선언
spring.web.resources.static-locations=classpath.static/
spring.mvc.static-path-pattern=/static/**
spring.web.resources.add-mappings=true

# resources/log/logback-local.xml 설정 파일로 지정
logging.config=classpath:log/logback-local.xml